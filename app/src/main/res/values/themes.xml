<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.MultiWindow" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->

        <item name="android:windowBackgroundFallback">@color/pink</item>
        <item name="android:windowBackground">@color/pink</item>
    </style>

    <style name="Theme.MultiWindow" parent="Base.Theme.MultiWindow" />
</resources>